
{% extends 'layout.jinja2' %}


{% block content %}
  

  <div class="container">
    {% include 'messages.jinja2' %}
    <h1 id="titol_de_seccio">{{ title }}</h1>
    <table>
    {% for alumne in alumnes %}
        <tr>
            <td>{{ alumne.assignacio["Alumne"] }}</td>
            <td>{{ alumne.assignacio["Pràctica"] }}</td>
            <td>{{ alumne.assignacio.professor }}</td>
            <td><a href="{{ url_for('alumnes_bp.editar_alumne', usuari=alumne.nom_de_usuari) }}"><button>Editar</button></a>
            <td>
              <form method="POST" action="{{ url_for('alumnes_bp.eliminacio_de_alumne', usuari=alumne.nom_de_usuari) }}">
                <button type="submit">Eliminar</button>
              </form>
        </tr>
    {% endfor %}
    </table>
    <a href="{{ url_for('assignacions_bp.assignar_automaticament') }}"><button>Realitzar Assignació Automática</button>
  </div>

{% endblock %}